-- Sacramento Buy Nothing — database SQL
--
-- EXISTING PRODUCTION DATABASE (run once after deploying July 2026 release):
--   scripts/supabase-migration-july-2026.sql
--   Adds Go Get tables, pickup attribution, violation lockout, updates + news.
--   Safe to re-run.
--
-- REPEAT EVENTS (seriesId column — schema only, no event rows):
--   scripts/supabase-migration-event-series.sql
--   Safe to re-run.
--
-- OPTIONAL ONE-OFF EVENT SEEDS (Lucid Winery 2026 + Fremont Aug 1):
--   scripts/seed-lucid-fremont-events-2026.sql
--   Not included in complete-schema.sql. Safe to re-run.
--
-- FULL SCHEMA (fresh install, new Supabase project, or full re-apply):
--   complete-schema.sql  (this repo root)
--   Entire site schema, RLS, triggers, seeds, realtime — safe to re-run.
--   Includes repeat-event seriesId on community_events (no sample events).
--
-- Paste either file into Supabase Dashboard → SQL Editor → New query → Run.
